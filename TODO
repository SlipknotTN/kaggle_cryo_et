# TODO: Debug input format for baseline hessian function (float?! range?!)
# TODO: Test extract coords v2 on the whole annotated volume at one time instead of single particles
(add break to test only one particle at the beginning)